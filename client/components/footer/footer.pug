.mini-stat(*ngIf='!hidden && (perUser || daily)')
  p.text-center
    span.hidden-xs(*ngIf='perUser && perUser.total')
      .fa.fa-square
      | &nbsp;&nbsp;
      span(*ngIf='perUser.today==0 || perUser.today==perUser.total')
        | Bugüne kadar
        span.count {{ perUser.total | number }}
        | troll ekledik.
      span(*ngIf='perUser.today!=0 && perUser.today!=perUser.total')
        | Bugün
        span.count {{ perUser.today | number }}
        | toplamda
        span.count {{ perUser.total | number }}
        | troll ekledik.
    span(*ngIf='daily && daily.total')
      .fa.fa-square
      | &nbsp;&nbsp;
      span(*ngIf='daily.today==0 || daily.today==daily.total')
        | Bugüne kadar troll hesapları
        span.count {{ daily.total | number }}
        | kez engelledik.
      span(*ngIf='daily.today!=0 && daily.today!=daily.total')
        | Troll hesapları bugün
        span.count {{ daily.today | number }}
        span.hidden-xs
          | toplamda
          span.count {{ daily.total | number }}
        | kez engelledik.
    a.fa.fa-remove.text-danger.pointer((click)='hide()')